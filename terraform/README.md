# Terraform Implementation

This directory holds actual Terraform implementation code when this
repository is used as a project template.

- Layout and rules: `.agents/skills/terraform-project-guidelines/references/directory-layout.md`
- Start new root modules from `templates/terraform-root-module/`.
- Start new project child modules from `templates/terraform-child-module/`.
