/**
 * Shared press-feedback utility classes (#125).
 * Defined in `globals.css` as `.mogu-touch-*`.
 */
export const touchCardClass = "mogu-touch-card";
export const touchRowClass = "mogu-touch-row";
export const touchIconClass = "mogu-touch-icon";
/** Minimum 44×44px hit target (iOS HIG). */
export const touchTargetClass = "mogu-touch-target";
